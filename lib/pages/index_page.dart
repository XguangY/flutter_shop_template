import 'package:flutter/material.dart';

class IndexPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('flutter shop template'),
      ),
      body: Center(
        child: Text('flutter shop template'),
      ),
    );
  }
}
